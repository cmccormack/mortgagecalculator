import { DefaultTheme } from "styled-components";

const theme: DefaultTheme = {
  borderRadius: "5px",
  padding: "1rem",
  colors: {
    main: "#444",
    secondary: "#FFF",
  },
};

export { theme };
